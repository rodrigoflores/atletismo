
<table>
  <tr>
    <th>Nome</th>
    <th></th>
  </tr>
  <% @atletas.each do |atleta| %>
    <tr>
      <td><%= atleta.user.name %></td>
      <td><%= link_to treinador_atleta_treinos_path(atleta)
    </tr>
  <% end %>
</table>

